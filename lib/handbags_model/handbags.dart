import 'package:flutter/material.dart';

class HandBags {
  String name;
  String image;
  String price;
  Color color;
  String type;
  String size;
  String about;
  HandBags(this.name, this.image, this.price, this.color, this.type, this.size,
      this.about);
}
